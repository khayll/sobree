---
"@sobree/core": patch
---

Make the optional version badge legible on any background — render it as
a pill with its own background + shadow, so bare grey text no longer
vanishes where the badge overlaps a dark region.
